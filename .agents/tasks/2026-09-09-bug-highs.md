# Task: Bug Highs — RCE, hangs, corruption (C1, H1, H6, H3+M1)

**Filename**: `tasks/2026-09-09-bug-highs.md` — date = creation date (approval date), never changes; summary file on completion: `tasks/2026-09-09-bug-highs_summary.md`.

---

## Context

- Highest-severity bugs from `bug-findings.md`, all confirmed in `bug-findings-review.md` (26.5/28 confirmed; H2 explicitly disputed → **excluded**). These are correctness/data-loss/RCE, all in the launch → poll → sidecar hot path, so they land as one coherent change.
- C1 is the only Critical (RCE via preamble comment injection). Review adds: the Claude inline preamble (`index.ts:~1136-1140`, bypasses `scriptPreambleFor`) must be covered too — sanitize at the sink.
- H1 (dead tab hangs watcher forever), H6 (parallel resume double-opens one `.jsonl`, ~5-line fix), H3+M1 (torn `.exit`/`.done` signals; two halves of one protocol fix — review: land together).
- Depends on: Phase-0 snapshots (pin current commands first). Blocks: refactor Phase-2 (which reshuffles the same builders — land bug fixes first so refactor preserves them).
- Links: `bug-findings.md` (C1, H1, H6, H3, M1), `bug-findings-review.md` (Confirmed §§, Missing #1/#5, Recommended order #1–4), `AGENTS.md` rules 2–3.

---

## Task-Description

1. **C1 — preamble injection (RCE).** Sanitize at the sink in `kitty.ts:sendLongCommand`: strip `[\r\n]` per preamble line (covers all three sites: launch, resume, Claude-inline). Additionally validate `params.name`/`params.cwd` at the `subagent` tool boundary (reject control chars). Cover all three preamble sites in tests, not just the helper.
2. **H1 — tab-death detection.** In `kitty.ts:pollForExit` probe liveness (every tick or after K consecutive `get-text` failures, injected `exists` fn for tests): `windowExistsOrNull === false` → return `{ reason: "error", exitCode: 1, errorMessage: "tab closed…" }`; `null` (control-plane unknown, N3) → keep polling. Applies to both `watchSubagent` and `monitorKeptTab` callers.
3. **H6 — resume reservation.** Reserve synchronously on the resume path before the first `await` (reuse `reservedNames`/`SubagentStore.reserve`, e.g. `resume::<artifactDir>::<name>`), re-check running + kept guards after the shell-ready delay immediately before send, release on failure. Per Missing #5: assert the reservation *before* the stale `.done`/`.exit` unlink so ordering is structural.
4. **H3+M1 — atomic sidecars.** Child side (`subagent-done.ts`): write `.exit`/`.done` via tmp+rename (reuse `writeAskSignalAtomic` pattern). Parent side (`kitty.ts:takeCompletionSidecar`): rename-claim `.done` exactly like `.exit` (today `.done` is check-then-act `existsSync`+`rmSync`).
5. Add/extend `test/bugfixes.test.ts` describes per the report's Tests table (C1 preamble, H1 liveness, H6 single-launch, H3 round-trip, M1 deliver-once). H2 test: **do not add** (disputed, passes on current code).

---

## Files to Touch

- **Sink + validation:** `pi-extension/subagents/kitty.ts` (`sendLongCommand`, `pollForExit`, `takeCompletionSidecar`), `pi-extension/subagents/launch.ts` (`scriptPreambleFor`), `pi-extension/subagents/subagent-done.ts` (atomic `.exit`/`.done` writes).
- **Guards:** `pi-extension/subagents/index.ts` (subagent boundary validation ~1700–1830; resume branch ~2180–2360: reservation + unlink ordering).
- **Tests:** `test/bugfixes.test.ts`, `test/launch.test.ts` (preamble block).

---

## Acceptance Criteria

- [ ] Preamble with `name: "a\ntouch /tmp/x\n#"` (plus `\n` in cwd/session paths, plus Claude path) emits no executable lines; tool boundary rejects control chars in `name`/`cwd`.
- [ ] `pollForExit` with injected `exists: () => false` resolves an error promptly; `() => null` keeps polling (N3 preserved).
- [ ] Two concurrent resumes for one name produce a single launch (second steers or errors; one `createSurface` per session file); reservation precedes stale-sidecar unlink.
- [ ] Child sidecar write→parent claim round-trip survives forced interleave; concurrent `takeCompletionSidecar` on one `.done` delivers once.
- [ ] `npm test` green; no registry/sidecar format change (rule 3); no watcher-semantics change (fire-and-forget stays).

---

## Notes

- Env order is cosmetic — do not "fix" order here (that's the refactor Phase-2 task with snapshots).
- H2 (registry mutex/queue) is **out of scope** — disputed false positive in-process (sync RMW); do not add dead synchronization.
- T4's future `cleanupClaudeSentinelFiles` must reuse existing `cli/claude.ts:cleanupClaudeSentinel` — do not add a second cleanup fn.
